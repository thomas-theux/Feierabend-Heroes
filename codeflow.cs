

// ✅ InitializeUI()
        // ✅ Add all skills to arrays
        // ✅ Initially set basics/skills array

    // ✅ OnEnable()
        // ✅ Reset currentIndex
        // ✅ DrawCards();
        // ✅ DisplayCursor();
        // ✅ DisplayCards();
        // ✅ UpdateOrbCount();

    // ✅ GetInput()
        // ✅ Get input from controller
        // ✅ Count currentIndex up/down
        // ✅ DisplayCursor();
        // ✅ BuySkill();

    // ✅ DisplayCursor()
        // ✅ Set position of cursor to currentIndex

    // DrawCards()
        // ✅ Set basics array from overall basics array
        // Set specials array from overall specials array
        // ✅ Pick random number
        // ✅ Add number to drawn cards array
        // ✅ Remove number from basics/specials array

    // ✅ DisplayCards()
        // ✅ Show title, info, cost from index 0, 1, 2 of drawn cards array

    // ✅ BuySkill()
        // ✅ PayWithOrbs();
        // ✅ UpdateOrbCount();
        // ✅  ImproveSkill();
        // ✅ IncreaseSkillLevel();
        // ✅ DrawCards();
        // ✅ DisplayCards();

    // ✅ PayWithOrbs()
        // ✅ Decrease orb count of player

    // ✅ UpdateOrbCount()
        // ✅ Show new orb count of player

    // ✅ ImproveSkill()
        // ✅ Call script that improves this skill

    // ✅ IncreaseSkillLevel()
        // ✅ Increase skill level count
        // ✅ Check if skill level is maxed
            // ✅ Remove this skill from overall basics/specials array